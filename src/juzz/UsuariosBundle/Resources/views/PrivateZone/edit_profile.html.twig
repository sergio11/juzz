{% extends 'juzzUsuariosBundle:PrivateZone:base.html.twig' %}

{% block tabContent %}

    
    <div id="changeEmail" class="modal fade in">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                  <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">×</span>
                    <span class="sr-only">Close</span>
                  </button>
                  <h4 class="modal-title">Cambia Tu Correo.</h4>
                </div>
                {{ form_start(change_email_form,{'action': path('editar_perfil',{user:user.nick}), 'method': 'POST'}) }}
                <div class="modal-body">
                    {{ form_row(change_email_form.email) }}
                </div>
                <div class="modal-footer">
                    {{ form_row(change_email_form.cambiar) }}
                </div>
                {{ form_end(change_email_form) }} 
            </div>
        </div>
    </div>


    <div id="changeEmail" data-urlorigin="" class="modal fade in" tabindex="-1" role="dialog" aria-labelledby="modalLabel" aria-hidden="false">
    <div class="modal-dialog">
        <div class="modal-content">
            
            
        </div>
      </div>
    </div>



    <div class="container">

        {{ render_flash_alerts() }}
        <!--<div class="row">
            
            <div class="col-md-3">
                <h2 class="section-title text-center">Mi Perfil</h2>
            </div>
            <div class="col-md-3 col-md-offset-4">
                <h4 class="text-center">Te uniste el : {{ user.ingreso | date('Y-m-d') }}</h4>
            </div>
        </div>-->
        
        <div class="container-card">
            {{ form_start(edit_form,{'action': path('editar_perfil',{user:user.nick}), 'method': 'POST'}) }}
            <legend>Editar Perfil</legend>
            {{ form_errors(edit_form) }}
            <div class="row">
                <div class="col-md-7 col-xs-12">
                    {{ form_row(edit_form.nick) }}
                    {{ form_row(edit_form.nombre) }}
                    {{ form_row(edit_form.ape1) }}
                    {{ form_row(edit_form.ape2) }}
                    
                </div>
                <div class="col-md-5 col-xs-12">
                    {{ form_row(edit_form.genero) }}
                    {{ form_row(edit_form.origen) }}
                    {{ form_row(edit_form.categoria) }}
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    {{ form_row(edit_form.descripcion) }}
                    {{ form_row(edit_form.avatar) }}

                </div>
            </div>
            <div class="row">
                <div class="col-md-8">
                    <a data-toggle="modal" href="#changeEmail">
                        <span class='glyphicon glyphicon-envelope'></span>&nbsp;
                        <span class="hidden-sm">cambiar Email</span>
                    </a>
                </div>
                <div class="col-md-4">
                    {{ form_rest(edit_form) }}
                </div>
            </div>
            {{ form_end(edit_form) }}
            
        </div>

    </div>
	
	
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    
    <script type="text/javascript">
        $(function(){
            $("select").select2({dropdownCssClass: 'dropdown-inverse'});
        })
    </script>
{% endblock %}

